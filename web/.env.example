# API Gateway endpoint (from CDK output: ApiEndpoint)
# This is used by the deployed CloudFront frontend
NEXT_PUBLIC_API_ENDPOINT=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod

# AWS Configuration (for local development only)
AWS_REGION=us-east-1

# Bedrock Agent Configuration (Get from: cdk deploy outputs)
# Only needed for local development with Next.js API routes
AGENT_ID=your-agent-id-here
AGENT_ALIAS_ID=your-alias-id-here

# Optional: If using named profile
# AWS_PROFILE=default
